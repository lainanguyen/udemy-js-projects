var firstName = prompt("Enter first name");
var secondName = prompt("Enter second name");

var n = Math.random();
n = Math.floor(n*=100)+1;

alert( firstName + " and " + secondName + " are " + n + "% compatible!");
